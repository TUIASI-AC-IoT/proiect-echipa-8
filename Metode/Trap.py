from tkinter import messagebox

def TrapRun():
    conectatAgent = 0
    if conectatAgent==1:
        #implementare Trap
        conectatAgent=0
    else:
        messagebox.showinfo("Error", "Nu sunteti conectat la agent.")